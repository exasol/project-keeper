name: Test Linux build on windows

on:
  push:

jobs:
  build-on-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Set up Java
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Cache local Maven repository
        uses: actions/cache@v2
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-
      - name: Build project-keeper
        run: mvn clean package -DskipTests
      - name: Upload artifact
        uses: actions/upload-artifact@master
        with:
          name: jars
          path: target/*.jar

  run-on-windows:
    runs-on: windows-latest
    needs: build-on-linux
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Set up Java
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Download jar
        uses: actions/download-artifact@v2
        with:
          name: jars
      - name: ls
        run: ls
      - name: install project-keeper
        run: mvn install:install-file -Dfile="project-keeper-maven-plugin-0.8.0.jar" -D pomFile="pom.xml"
      - name: run project-keeper
        run: mvn project-keeper:verify