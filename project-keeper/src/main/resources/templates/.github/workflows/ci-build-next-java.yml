# Generated by Project Keeper
# https://github.com/exasol/project-keeper/blob/main/project-keeper/src/main/resources/templates/.github/workflows/ci-build-next-java.yml
name: CI Build next Java
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  next-java-compatibility:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: "bash"
    permissions:
      contents: read
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up JDK $nextJavaVersion
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: $nextJavaVersion
          cache: "maven"
      - name: Run tests and build with Maven $nextJavaVersion
        run: |
          mvn --batch-mode clean package -DtrimStackTrace=false $skipNativeImage -Djava.version=$nextJavaVersion
